import { redirect } from 'next/navigation'

/**
 * Root-level 403 page that redirects to locale-specific version
 */
export default function Root403Page() {
  // Redirect to default locale (Russian)
  redirect('/ru/403')
}
